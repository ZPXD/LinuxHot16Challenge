# AUTOR: XyderDlugus

# HOT 16

# 1. Cd /var/www/flaga
# Opis: Change Directory, zmiana ścieżki na której aktualnie jesteśmy na inną, w tym przypadku zmiana na ścieżkę /var/www/flaga
# 2. Mkdir flaga
# Opis: Make Directory, tworzy nowy katalog, w tym przypadku jest to katalog o nazwie flaga
# 3. Rmdir flaga
# Opis: Remove Directory, usuwa katalog flaga
# 4. Cat <plik.txt>
# Opis: Za pomocą polecenia cat, wyświetlamy zawartość danego pliku
# 5.nano [nazwa pliku]
# Opis:uruchamia terminal, w którym możemy tworzyć własne ciągi znaków lub po prostu pisać kod
# 6. Sudo [komenda]
# Opis:Używa się tego polecenia zazwyczaj jak chcemy nadać sobie na chwilę uprawnienia roota, do wykonania jakiejś operacji
# 7. ls 
# Opis: Komenda służąca wyświetleniu plików znajdujących się w danym miejscu
# 8. Apt-get [install, update, upgrade etc.] 
# Opis: Komenda pozwalająca na instalację lub podniesienie wersję wyżej danego pakietu.
# 9. Touch [nazwa pliku]
# Opis:Tworzy pusty plik tekstowy
# 10.cp [nazwa pliku] [lokalizacja katalogu]
# Opis: Kopiuje plik z jednego katalogu do innego
# 11. tar -xvf archiwum.tar
# Opis:Rozpakowuje archiwum tar
# 12.lshw
# Opis: Wyświetlenie wszystkich informacji o komputerze - płycie głównej, procesorze i jego architekturze, etc
# 13. free
# Opis:Wyświetlenie informacji o dostępnej pamięci w kilobajtach
# 14. ifconfig
# Opis: Wyświetla informacje o twoim IP
# 15.net lookup [adres strony]
# Opis: Wyszukuje adres twojej strony internetowej
# 16. pwd
# Opis:Wypisuje ścieżkę obecnego katalogu

# HOT 32
# 17. Chmod [plik]
# Opis: Zmiana praw dostępu do danego pliku
# 18. Which
# Opis:Wypisuje gdzie znajduje się plik z programem o podanej nazwie
# 19. Env
# Opis:Wypisuje aktualne wartości wszystkich zmiennych środowiskowych
# 20. kill [PID]
# Opis: Zabija dany proces o podanym numerze PID
# 21. Ps
# Opis: Wyświetla procesy aktualnie uruchomione na komputerze użytkownika
# 22. Exit
# Opis: Komenda służąca opuszczeniu terminala
# 23. Clear
# Opis: Czyści ekran terminalu
# 24. History
# Opis: Wyświetla listę ostatnio użytych komend
# 25. Nslookup
# Opis:Wyszukuje serwery DNS
# 26. grep [-opcje] wzorzec [plik lub ścieżka]
# Opis:Narzędzie służące do wyszukiwania według zadanych wzorców
# 27. chown nowy_wlasciciel nazwa_pliku
# Opis:  daje prawa do pliku innemu użytkownikowi
28. man [komenda]
# Opis: Wyświetla opis jak używać dane polecenie
# 29. head [-liczba] [plik] 
# Opis: Wyświetla początkowe słowa w danym pliku zależne od -liczba np. jeśli mamy -20, to wyświetla 20 słów
# 30. Tail [-liczba] [plik] 
# Opis: Wyświetla końcowe słowa w danym pliku zależne od -liczba np. jeśli mamy -20, to wyświetla 20 słów
# 31. MV [plik] [katalog]
# Opis:Służy do przeniesienia pliku do innego folderu.
# 32. MV [stara_nazwa_pliku] [nowa_nazwa_pliku]
# Opis: Zmiana nazwy pliku na inną
